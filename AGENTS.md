Always use the provided `DESIGN.md` file to guide your decisions. If there's a conflict between this file and the user's request, prioritize the design system.

Always use the `Proiect_Securitate_Digitala_unlocked.pdf` file check the requirements and the design system.

backend will be in `backend` directory
frontend will be in `frontend` directory

postgresql will be in `db` directory